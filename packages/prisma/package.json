{
  "name": "@everlast/prisma",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "studio": "prisma studio",
    "seed": "prisma db seed"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^5.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "prisma": {
    "seed": "pnpm exec ts-node seed.ts"
  }
}
